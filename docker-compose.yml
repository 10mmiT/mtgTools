services:
  mtgtools:
    image: ${DOCKERHUB_USERNAME:-local}/mtgtools:latest
    build: .
    ports:
      - "3000:3000"
    volumes:
      - mtgtools-data:/app/data
    env_file:
      - .env

volumes:
  mtgtools-data:
